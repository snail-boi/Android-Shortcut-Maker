<Window x:Class="android_shortcut_maker.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Device Settings"
        Width="500"
        Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Device Name:"/>
        <TextBox x:Name="TxtName" Grid.Row="0" Grid.Column="1" Margin="0,2,0,6" />

        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="USB Serial:"/>
        <TextBox x:Name="TxtUsb" Grid.Row="1" Grid.Column="1" Margin="0,2,0,6" />

        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="Wi-Fi (ip:port):"/>
        <TextBox x:Name="TxtWifi" Grid.Row="2" Grid.Column="1" Margin="0,2,0,6" />

        <TextBlock x:Name="TxtStatus" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,4,0,0" Foreground="Gray" TextWrapping="Wrap" />

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="BtnAuto" Width="120" Margin="0,0,8,0" Content="Auto Detect" Click="BtnAuto_Click" />
            <Button Width="90" Margin="0,0,8,0" Content="Save" Click="BtnSave_Click" />
            <Button Width="90" Content="Cancel" Click="BtnCancel_Click" />
        </StackPanel>
    </Grid>
</Window>
