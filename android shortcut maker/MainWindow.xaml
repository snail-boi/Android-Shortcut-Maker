<Window x:Class="android_shortcut_maker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:android_shortcut_maker"
        mc:Ignorable="d"
        Title="android shortcut maker" Height="590" Width="760"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Window.Resources>
        <!-- Brushes and implicit control styles are now in App.xaml. -->
        <!-- Only MainWindow-specific keyed resources remain here.    -->

        <Style TargetType="Button" x:Key="PrimaryButton" BasedOn="{StaticResource {x:Type Button}}"/>

        <DataTemplate x:Key="AppTemplate" DataType="{x:Type local:AppInfo}">
            <Grid Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border Width="40" Height="40" CornerRadius="8"
                        Background="{DynamicResource IconTileBrush}"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image Source="{Binding IconPath}" Width="40" Height="40" Stretch="UniformToFill">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IconPath}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>
                </Border>

                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding DisplayName}" FontSize="14" FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding PackageName}" FontSize="12"
                               Foreground="{DynamicResource SubtleTextBrush}"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <TextBlock Grid.Column="2" Text="›" FontSize="18"
                           Foreground="{DynamicResource ChevronBrush}"
                           VerticalAlignment="Center" Margin="10,0,6,0"/>
            </Grid>
        </DataTemplate>
    </Window.Resources>


    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button x:Name="SettingsButton" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" Click="SettingsButton_Click">Settings</Button>
                <Button x:Name="ThemeToggleButton" Style="{StaticResource PrimaryButton}" Click="ThemeToggleButton_Click">Dark Mode</Button>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="android shortcut maker" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" />
                <TextBlock Text="Create scrcpy shortcuts for installed Android apps" Foreground="{DynamicResource SubtleTextBrush}" FontSize="12" />
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="RefreshButton" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" Click="RefreshButton_Click">Refresh</Button>
                <Button x:Name="GenerateIconsButton" Style="{StaticResource PrimaryButton}" Margin="0,0,0,0" Click="GenerateIconsButton_Click">Generate App Icons</Button>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Margin="0,12,0,12" Background="{DynamicResource CardBrush}" CornerRadius="12" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" VerticalAlignment="Center" Foreground="{DynamicResource SubtleTextBrush}" FontSize="12">Double-click an app to create a shortcut</TextBlock>

                <ListBox Name="AppsList" Grid.Row="1" MouseDoubleClick="AppsList_MouseDoubleClick" Background="Transparent" BorderThickness="0" ItemTemplate="{StaticResource AppTemplate}" />
            </Grid>
        </Border>

        <TextBlock x:Name="FooterStatusText" Grid.Row="2" HorizontalAlignment="Left" Margin="6,8,0,0" Foreground="{DynamicResource SubtleTextBrush}" FontSize="11" TextWrapping="Wrap" />
    </Grid>
</Window>
